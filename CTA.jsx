import { site } from "../lib/data";
export default function CTA({title="Ready for a detail that feels premium?", text="Call or text Dripped Auto Detailing to request a quote, choose a package, and get mobile service brought directly to you."}){return <section className="cta"><div><span className="eyebrow">Book Dripped</span><h2>{title}</h2><p>{text}</p></div><div className="cta-actions"><a className="primary big" href={site.phoneHref}>Call {site.phone}</a><a className="ghost big" href={`${site.smsHref}?body=Hi, I want a quote for a detail.`}>Text For Quote</a></div></section>}
